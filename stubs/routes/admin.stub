<?php
use Illuminate\Support\Facades\Route;
use $PLUGIN_NAMESPACE$\$STUDLY_NAME$\$CONTROLLER_NAMESPACE$\$STUDLY_NAME$Controller;

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider within a group which
| contains the "web" middleware group. Now create something great!
|
*/

Route::middleware(['auth:admin'])->group(function () {
	Route::prefix('$LOWER_NAME$')->as('$LOWER_NAME$.')->group(function() {
		Route::get('/', [$STUDLY_NAME$Controller::class,'index']);
		Route::get('/create', [$STUDLY_NAME$Controller::class,'create'])->name('create');
		Route::get('/edit', [$STUDLY_NAME$Controller::class,'edit'])->name('edit');
		Route::get('/show', [$STUDLY_NAME$Controller::class,'show'])->name('show');
		Route::post('/store', [$STUDLY_NAME$Controller::class,'store'])->name('store');
		Route::put('/update', [$STUDLY_NAME$Controller::class,'update'])->name('update');
		Route::post('/destroy', [$STUDLY_NAME$Controller::class,'destroy'])->name('destroy');
	});
});
